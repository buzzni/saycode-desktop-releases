# Local agent/session artifacts — never commit to this public release repo
memory/
.aplus/

# macOS
.DS_Store

# auto-added by aplus-dev-studio (block env secrets)
.env

# auto-added by aplus-dev-studio (block env secrets)
.env.*
