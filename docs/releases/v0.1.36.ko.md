# Saycode Desktop v0.1.36

배포일: 2026-08-18

<!-- desktop-source-commit: e2726318586ae4da185d7dc29b6ec3b6de6b6ccc -->
<!-- previous-desktop-source-commit: e875f715ade2aa7bc4c319ec8d24992cd348e70e -->

## 새 기능

### 대화 실행 중에도 다음 지시를 미리 써두기

에이전트가 응답하는 동안 다음 지시를 입력하면 사라지지 않고 대화창 위에 대기열로

#### 사용 방법

평소처럼 입력하고 Enter를 누르면, 실행 중일 때는 자동으로 대기열에 들어갑니다.

### 도구가 만든 이미지를 대화에서 바로 보기

에이전트가 화면을 캡처하거나 이미지 파일을 읽으면, 그 그림이 대화 안에 썸네일로

#### 사용 방법

따로 켤 것은 없습니다. 썸네일을 누르면 원본 크기로 크게 볼 수 있고 Escape 로 닫습니다.

### 새 대화에서 Grok 에이전트를 고를 수 있습니다

새 대화를 시작할 때 고를 수 있는 에이전트에 Grok(xAI)이 추가되었습니다. Claude·Codex·opencode와 같은 자리에서 선택할 수 있고, 선택한 에이전트는 다음에 새 대화를 열 때도 그대로 유지됩니다.

#### 사용 방법

1. 프로젝트에서 새 대화를 엽니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/582)

### 선택 설치형 프로젝트 템플릿 확장 준비

프로젝트 템플릿을 공식 확장으로 분리할 수 있도록 안전한 표시 정보·자산 계약과 checksum 검증 설치 경로를

#### 사용 방법

새 프로젝트의 템플릿 탭에서 `템플릿 확장 설치`를 누르고 설치를 확인한 다음 `템플릿 확장 활성화`를 누릅니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/585)

### 프로젝트 설정에 SSO 로그인 탭 추가

지금까지 프로젝트에 조직 SSO 로그인을 붙이려면 웹으로 나가야 했습니다. 이제 데스크탑

#### 사용 방법

회사 프로젝트의 설정을 열고 개발자용 그룹의 **SSO 로그인** 탭을 선택합니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/576)

### 프로젝트에서 실행 중인 프로세스 확인

프로젝트와 워크트리 안에서 지금 무엇이 실행 중인지(명령, PID, 사용 중인 포트, 소속 워크트리) 한눈에 볼 수 있고, 필요 없는 프로세스는 그 자리에서 종료할 수 있습니다. 워크트리에서 띄운 개발 서버가 포트를 계속 잡고 있을 때 앱을 벗어나지 않고 정리할 수 있습니다. 종료는 항상 확인을 거치며, 종료 요청에도 살아남은 프로세스에만 강제 종료가 나타납니다.

#### 사용 방법

프로젝트 화면 오른쪽 위 ⋯ 메뉴에서 "프로젝트 프로세스 보기"를 누르거나, Cmd+P 팔레트에서 "프로세스"를 검색하세요. 열려 있는 동안 10초마다 자동으로 갱신됩니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/567)

### PowerPoint·Excel 파일을 앱에서 바로 봅니다

지금까지 `.pptx`·`.xlsx` 파일을 열면 "텍스트로 디코딩할 수 없는 파일입니다."만 뜨고 내용을 볼 수

#### 사용 방법

프로젝트 파일 목록이나 채팅에 올라온 `.pptx`·`.xlsx` 파일을 누르면 오른쪽 뷰어에 바로 나타납니다.

## 변경된 기능

### 글자 크기가 표준 스케일로 정돈됐습니다

일부 화면에서 제각각이던 글자 크기(9~21px 사이 14종)를 표준 스케일로 통일했습니다. 대부분

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/563)

### 답변은 결론부터 — 작업 과정은 "N분 M초 동안 작업" 한 줄로 접힙니다

대화 화면에서 한 번의 요청에 대한 답변이 끝나면, 그 사이의 진행 과정(중간 설명, 생각 과정, 도구 실행)이

#### 사용 방법

별도 설정 없이 자동 적용됩니다. 과정을 보고 싶으면 답변 위의 `... 동안 작업` 줄을 클릭해 펼치고, 다시 클릭하면

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/573)

### 새 대화의 실행 옵션 프로필이 마지막에 고른 것으로 미리 선택됩니다

새 대화 화면의 "프로필"이 매번 "프로필 없음"으로 돌아가지 않고, 마지막에 골랐던 프로필로 미리 선택된 채 열립니다. 선택한 프로필의 에이전트·모델·강도·worktree 설정도 함께 적용되므로, 같은 조합으로 계속 일할 때 매번 다시 고를 필요가 없습니다.

#### 사용 방법

새 대화 화면의 "프로필"에서 원하는 프로필을 한 번 고르면 됩니다. 다음부터 새 대화를 열 때 그 프로필이 선택된 상태로 나타납니다. 기본 선택을 없애려면 "프로필 없음"을 고르세요.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/562)

### 작업이 끝나면 진행 과정이 자동으로 접힙니다

AI가 답변을 완성하면, 그 과정에서 실행된 도구 호출·사고 과정 메시지들이 "진행 과정 N단계" 한 줄로

#### 사용 방법

별도 설정 없이 자동 적용됩니다. 접힌 "진행 과정 N단계" 줄을 클릭하면 펼쳐지고, 다시 클릭하면

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/563)

### 채팅 화면이 더 넓고 읽기 편해졌습니다

AI 답변과 시스템 메시지를 감싸던 상자(테두리·배경)를 없애 본문이 화면을 넓게 쓰도록 바꿨습니다.

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/563)

### 터미널이 실행 중일 때 새로고침 전에 확인을 요청

지금까지는 새로고침(Cmd+R, 메뉴의 Reload)을 누르면 실행 중이던 원격 터미널이 모두 예고 없이 종료됐습니다. 이제 살아 있는 터미널이 하나라도 있으면 "실행 중인 터미널 N개가 종료됩니다"라는 확인 창이 먼저 뜨고, 취소하면 새로고침이 진행되지 않습니다. 터미널이 없을 때는 이전과 똑같이 바로 새로고침됩니다.

#### 사용 방법

별도 설정 없이 자동 적용됩니다. 확인 창에서 취소를 누르면 터미널이 그대로 유지됩니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/567)

### 터미널이 항상 예측 가능한 위치에 열립니다

터미널을 여는 위치가 진입점마다 달랐던 문제를 하나의 규칙으로 통일했습니다: 이미 열린 터미널이

#### 사용 방법

별도 설정 없이 자동 적용됩니다. 채팅 하단의 터미널 버튼과 프로젝트 헤더의 "터미널 열기"는 기존

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/563)

### GitHub 트리거에서 AgentTask 리뷰·후속 반영 지원

서버 자동화를 지원하는 프로젝트의 GitHub 트리거에서 AgentTask 리뷰를 선택할 수 있습니다.

#### 사용 방법

프로젝트 설정의 실행 > GitHub 트리거에서 액션을 `AgentTask 리뷰 · 원 작성 세션에 후속 반영`으로

### Plugin Manager를 공식 확장으로 설치해 사용할 수 있습니다

Plugin Manager 공식 확장을 앱에서 명시적으로 설치하고, 권한 승인과 활성화를 거쳐 `/plugin`에서 열 수

#### 사용 방법

`/plugin`을 실행한 뒤 안내 화면에서 설치, 권한 승인, 활성화를 순서대로 진행합니다. 설치하지 않은 경우에는

## 수정된 문제

### 극단적으로 긴 메시지도 빠르게 표시됩니다

AI의 극단적인 반복 출력(같은 말 수천 번)이 화면을 멈추지 않도록 보호합니다. 메시지가 같은 줄을 20회

#### 사용 방법

별도 설정 없이 자동 적용됩니다. 극단적인 반복 메시지를 받으면 "같은 줄이 N회 반복되어 접었습니다",

### 끊긴 대화의 질문 답변이 사라지지 않도록 수정

에이전트가 물어본 질문에 답변할 때, 그 대화의 실행이 이미 끊겨 있으면 답변이 전달되지 않고 조용히 사라지는 경우가 있었습니다. 앱이 대화 내용만 보고 "아직 진행 중"이라고 잘못 판단해 재연결 단계를 건너뛰던 것이 원인이었습니다. 이제 서버가 알려준 실제 실행 상태를 기준으로 판단해, 끊긴 대화라면 먼저 다시 연결한 뒤 답변을 전달합니다.

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/598)

### 대소문자가 다른 파일 경로도 대화에서 바로 열 수 있도록 수정

대화 내용에 언급된 파일 경로의 대소문자가 실제 저장소에 기록된 것과 다르면(예: 실제 `README.md`, 답변에는 `readme.md`) 링크가 만들어지지 않았습니다. 실제로는 열리는 파일인데도 클릭할 수 없었습니다. 이제 대소문자가 달라도 같은 파일로 보고 링크를 유지합니다.

#### 사용 방법

별도 설정 없이 자동 적용됩니다. 대화에 나온 파일 경로를 클릭하면 해당 파일이 열립니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/598)

### 사이드바 접힌 목록의 상태 표시가 다시 동그란 점으로 보입니다

사이드바에서 프로젝트 목록이나 프로젝트별 대화 목록을 접으면, 그 안에 완료된 대화나 입력을 기다리는 대화가 있을 때 제목 옆에 작은 점이 표시됩니다. 이 점이 옆으로 길게 늘어나 제목과 개수 사이를 가로지르는 타원 막대처럼 보이던 문제를 고쳤습니다. 이제 의도한 대로 작은 원으로 표시됩니다.

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/587)

### 새 대화가 준비되어도 보고 있던 화면을 뺏지 않습니다

새 대화를 보낸 뒤 준비되는 동안 다른 탭이나 다른 화면으로 옮기면, 대화가 실제로 시작될 때

#### 사용 방법

별도 설정 없이 자동 적용됩니다. 새 대화를 보낸 뒤 다른 작업을 하다가, 시작한 탭으로 돌아가면

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/574)

### 새 소식 화면 읽기 쉽게 표시

새 버전으로 켰을 때 뜨는 "새 소식" 창에서 내용이 태그 문자열로 보이던 문제를 고쳤습니다. 이제 제목·문단·목록·링크로 정리되어 보이고, PR 링크를 누르면 브라우저에서 열립니다.

#### 사용 방법

새 버전 실행 시 자동으로 뜨거나, 메뉴에서 새 소식을 선택하면 정리된 내용을 볼 수 있습니다.

### 열리지 않는 채팅 파일 링크 제거

프로젝트 채팅에서 자동으로 만들어지던 파일 링크가 실제로 열리는 파일에만 걸립니다.

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/596)

### 작은 창에서 채팅 영역이 눌려 아무것도 안 보이던 문제 수정

창을 가장 작게 줄인 상태(1040×700)에서 터미널을 아래에 열면, 채팅 영역이 필요한 최소 크기보다

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

### 좁은 창에서 분할이 접히는 빈도 감소

터미널이나 두 번째 패널을 아래에 열었을 때 화면이 곧바로 한 패널만 남도록 접히는 일이 크게 줄었습니다. 전에는 두 영역을 항상 반반으로 나눠서 각자 자기 최소 크기의 두 배를 버텨야 했고, 노트북 화면에서는 그 높이가 거의 나오지 않았습니다. 이제 각 영역이 실제로 필요한 크기에 맞춰 나뉩니다.

#### 사용 방법

분할은 그대로 사용하시면 됩니다. 화면이 좁아 한 패널만 보이게 접혔을 때 위쪽 안내 문구의 **전체 보기로 전환**을 누르면 원래 분할로 돌아옵니다.

### 최종 답변 뒤에도 응답 중으로 남는 문제 수정

최종 답변과 종료 신호가 도착했는데도 마지막 도구 카드와 입력창이 계속 응답 중으로 표시될 수 있던 문제를 수정했습니다. 이제 종료 신호가 실시간 연결로 먼저 도착해도 남은 메시지를 확인한 뒤 응답 상태가 정상적으로 끝납니다.

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/600)

### 터미널을 열면 오른쪽 위 버튼들이 사라지던 문제 수정

오른쪽 위 터미널 버튼을 누르면 터미널이 열리면서 방금 누른 그 버튼과 "새 대화", 에이전트 보드

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

### 프로세스 목록을 못 불러왔을 때 "없음"으로 보이던 문구 수정

프로젝트 프로세스 보기에서 목록을 불러오지 못했을 때, 실패 안내와 함께 "실행 중인 프로세스가

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

### 프로젝트를 열면 대화 목록이 항상 펼쳐집니다

사이드바에서 프로젝트 이름 줄을 눌러 대화 목록을 접어두면, 그 접힘 상태가 프로젝트를 바꿔도 그대로

#### 사용 방법

별도 설정 없이 자동 적용됩니다. 프로젝트를 클릭하면 최근 대화 목록이 바로 보이고, 화면을 넓게 쓰고

관련 변경: [PR 링크](https://github.com/buzzni/aplus-dev-studio-desktop/pull/581)

### 한글 PDF 글자 깨짐 수정

한글(및 일본어·중국어) 전용 글꼴로 만들어진 PDF를 미리보기로 열면 본문이 빈칸이나

#### 사용 방법

기존과 같습니다. 파일 목록이나 답변의 파일 링크에서 PDF를 열면 됩니다.

### AI 답변 주위에 남아 있던 상자 자국 제거

AI 답변과 시스템 메시지에서 상자(테두리·배경)를 없앴을 때, 그림자와 윗면 하이라이트가 그대로 남아

#### 사용 방법

별도 설정 없이 자동 적용됩니다.

## Download
- macOS (arm64) DMG: `Saycode-0.1.36-arm64.dmg`
- macOS (x64) DMG: `Saycode-0.1.36-x64.dmg`
- In-app update metadata: `latest-mac.yml`

## Checksum
```text
d149d67b30ad35f6fbfa3e9f915489fe11fce65dc6b02c74af0a678a4c317fa5  Saycode-0.1.36-arm64.dmg
dbfe1725f9e782044b2fc685f75dea0c65968e3c80ea2ee20184ae155129dec9  Saycode-0.1.36-x64.dmg
```
